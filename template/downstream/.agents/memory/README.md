# Reviewed Project Memory

Store Human-reviewed lessons, decisions, incidents, and historical context that reduce future orientation cost.

Critical behavior, interfaces, build commands, and compatibility rules must also live in code, tests, configuration, or formal project documentation. Memory is supporting context, not the only source of correctness.
