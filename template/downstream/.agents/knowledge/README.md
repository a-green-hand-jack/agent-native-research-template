# Project Knowledge

Store durable, project-specific facts here: architecture orientation, domain concepts, operational constraints, and verified local conventions.

Use one folder per entry with a `KNOWLEDGE.md` marker when the catalog grows. Keep reusable procedures in `.agents/skills/`, and keep executable project behavior in the functional package and CLI.
