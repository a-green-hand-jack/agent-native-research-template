schema_version: 1
id: smoke
question: Does the smallest complete project path execute successfully?
contribution: bootstrap
config: configs/base.yaml
environment: main
executor: local
evaluation: smoke
command: make smoke
seed_policy:
  mode: fixed
  seeds:
    - 0
budget:
  max_runs: 1
  max_wall_time_seconds: 60
stopping_rule:
  type: after_runs
  runs: 1
inclusion_criteria:
  - Include every completed smoke attempt.
artifacts: []
